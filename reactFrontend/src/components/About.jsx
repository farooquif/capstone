import React from "react";

const About = () => {
  return (
    <div>
      Welcome to the Capstone Project of Group *Git Happens*! The app serves as
      a searchable enterprise directory. When logged into the app, an employee
      may search for other employees by name, or instantly access a list of
      their direct reports. Information provided for employees includes name,
      job role, location, phone number, and optionally the salary if your
      permissions allow it.
    </div>
  );
};

export default About;
